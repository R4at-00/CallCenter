import React from 'react';
import { Button } from '@/components/ui/button'

export default function NewIncident(){
    return (
        <Button className="p-7 text-lg">Nueva incidencia</Button>
    );
}